# Returning-HTML-String-Using-Requests-Package-in-Python
This simple code returns the HTML string of a URL. Note: to install the Requests package, simple type "pip install requests" in your terminal. 
